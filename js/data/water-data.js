// Data for scenes and spawn points for all Water in game
// (currently only in stalagmites)

(function () {

'use strict';

var water_data = {
	'stalagmites' : {'x':284, 'y':388}
};

global.set('water-data', water_data);

}());